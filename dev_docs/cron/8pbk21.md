# cron job `8pbk21` — nanna-coder disposition

- **Job id**: `8pbk21`
- **Slice**: oldest 15 open issues by created_at across the routine's repo set
- **Tick date**: 2026-07-10
- **Prior tick (oldest-15 planner for this repo)**: `kgg7th` — [#484](https://github.com/DominicBurkart/nanna-coder/pull/484) (defers to #472)
- **Routine**: <https://dominic.computer/blog/2026/routines?format=md>

## Disposition

Fingerprint-only. No implementation changes on this branch.

nanna-coder contributes the following to the routine's oldest-15 slice:

- **#5** ("CI health monitoring", 2025-09-24)
- **#10** ("CI maintenance docs", 2025-09-24)
- **#20** ("Entity Management" umbrella, 2025-10-05)
- **#39** ("Migrate from Ollama to vLLM", 2025-12-25)

Note: #23 and #24 (AST / Testing entity sub-issues under #20) are
excluded from this slice per the routine's "blocked by incomplete prior
work" filter — PR #472 marks #20's sub-work as intentionally out of
scope until #20's scaffolding lands.

### #5, #10, #20 → PR #472

Covered by the in-flight primary implementation PR **#472**
(`[h12tqo][oldest-issue:5] docs(ci, architecture): scaffold CI
maintenance docs, entity-class prose, and ci-metrics seed`), head SHA
`e17d85d8d54bc34d8b5df228b4b2326a22147757`, base `main` at
`c71e6114d66c39dbf82525183e28d0a9fbb45f4f`.

PR #472 has been open since 2026-06-29, last touched 2026-07-05. Same
head SHA as recorded by prior ticks — no re-work triggered.

### #39 → no primary work yet

Issue **#39** (vLLM migration) has no in-flight implementation PR at
this tick. It is already assigned to `@DominicBurkart` (per issue
state), so the routine's "assign owner if unassigned + no prior work"
step is a no-op here. Implementation is intentionally deferred by this
planner:

- Migrating the model container from Ollama to vLLM is a multi-crate
  change touching `model/`, `harness/src/container.rs`, and the CI
  container-image pipeline.
- vLLM's Rocm / iGPU story is best-effort upstream. Doing the migration
  correctly needs live hardware for the ROCm path and a real GPU-less
  CPU fallback smoke test, neither of which a stateless cron slot can
  supply.
- Scope-safe next step: file a scoped sub-issue for CPU-only vLLM
  container skeleton (mirroring the current Ollama container's
  interface) before touching the harness wiring. That is a design task
  requiring maintainer input on interface compatibility — deferred to a
  future tick where it can be human-in-the-loop.

## Fingerprint table (issue → PR → head SHA)

| # | PR | head SHA |
| --- | --- | --- |
| 5 | #472 | `e17d85d8d54bc34d8b5df228b4b2326a22147757` |
| 10 | #472 | `e17d85d8d54bc34d8b5df228b4b2326a22147757` |
| 20 | #472 | `e17d85d8d54bc34d8b5df228b4b2326a22147757` |
| 39 | *no primary PR yet; assigned; deferred to future tick* | — |

## Promotion

`ready-for-review` intentionally NOT set — planner jobs never promote
per the routine contract. Janitor handles dedup + close.
