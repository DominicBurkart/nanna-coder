coverage:
  status:
    project: off
    patch:
      default:
        # Relaxed from 100%: run_mcp_http_server in main.rs requires a live
        # Ollama instance and bound network socket and is genuinely uncoverable
        # in unit CI.  Matches the threshold already established on main.
        target: 90%

ignore:
  - "harness/src/main.rs"
