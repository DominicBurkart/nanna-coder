[case]
id = "escalate-production-request"
name = "Requested production effect with no capable card"
description = "deployer is capped at sandbox; the planner asked for production, which no card in the catalog reaches."

[request]
parent_task_id = "task-10"
parent_task_description = "Promote the release"
parent_task_repo = "github.com/example/repo"
identity = "deployer"
subtask = "Promote build 42 to production."
dev_loop = "outer"
requested_effect = "production"

[expected]
verdict = "escalate"
reason_codes = ["effect_above_ceiling"]

[metadata]
tags = ["escalate", "no-capable-card"]
