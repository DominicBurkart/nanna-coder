[case]
id = "block-loop-mismatch-outer-to-inner"
name = "Outer card asked to write code"
description = "deployer acts in the outer loop; the spawn places it in the inner loop."

[request]
parent_task_id = "task-6"
parent_task_description = "Fix the login bug"
parent_task_repo = "github.com/example/repo"
identity = "deployer"
subtask = "Add a regression test for the empty-password path."
dev_loop = "inner"
requested_effect = "workspace"

[expected]
verdict = "block"
reason_codes = ["loop_mismatch"]

[metadata]
tags = ["block", "loop-mismatch"]
