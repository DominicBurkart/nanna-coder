# Cron `stdewg` — nanna-coder slice

- job-id: `stdewg`
- oldest-issue-id: `one_track#2`
- parent-fingerprint: `8t5mom` (PR #460)
- tick-date: 2026-06-24
- scope: oldest 15 author:DominicBurkart open issues across scoped repos
- slice: nanna-coder (6 issues: #5, #10, #20 epic, #23, #24, #39)
- sibling slices: velib-mcp (#9), one_track (#2–#9)

## Fingerprint result — byte-identical no-op vs `8t5mom`

| issue | canonical PR              | head SHA @ `8t5mom`     | head SHA @ `stdewg`     | Δ |
|------:|---------------------------|-------------------------|-------------------------|---|
| #5    | #247                      | `8a589694`              | `8a589694`              | — |
| #10   | #254                      | `d9ccf860`              | `d9ccf860`              | — |
| #20   | EPIC (no PR)              | n/a                     | n/a                     | — |
| #23   | #258                      | `7c2b7ed9`              | `7c2b7ed9`              | — |
| #24   | #267                      | `f229f365`              | `f229f365`              | — |
| #39   | #140 + #336               | `79c94397` / `e270a211` | `79c94397` / `e270a211` | — |

Base `main` unchanged at `c71e6114` since `yuzy9i`. **No canonical PR head moved between `8t5mom` and `stdewg`.**

## Per-issue disposition (unchanged from `8t5mom`)

| issue | disposition |
|------:|-------------|
| #5  | NO-OP — defer to #247 (`behind`; blocked on #243 + `workflows` token scope) |
| #10 | NO-OP — defer to #254 (`behind`; blocked on `workflows` token scope) |
| #20 | NO-OP — epic; defer to leaves #23, #24 |
| #23 | NO-OP — defer to #258 (`dirty`; codecov diff 92.70%) |
| #24 | NO-OP — defer to #267 (`behind`; codecov diff 80.88%; blocked on #23) |
| #39 | NO-OP — defer to #140 (`dirty`) + #336 (plan, `behind`) |

## Why this CRON ships no code

1. Canonical PRs exist for every code-bearing in-scope issue; parallel implementations create duplicate-PR sprawl (CRON rule 2).
2. #247 / #254 / #258 / #267 / #140 are partial-progress PRs authored by other agent jobs; this cron does not push to branches it does not author.
3. #254's blocker is a token-permission problem (`workflows` scope) — this cron's token also lacks that scope.
4. #258 (`dirty`) and #140 (`dirty`) need rebase by their original authors before any code can be layered.

## Janitor carry-over

- Rebase #247, #254, #267 against current `main` (`c71e6114`).
- Resolve conflicts on #258 (`dirty`) and #140 (`dirty`).
- #254's `docs-check` wiring needs a maintainer token with `workflows` scope.
- Close superseded `claude/affectionate-hawking-*` fingerprint PRs on sight — keep `stdewg` (this PR), close `8t5mom` (#460), and ALL earlier `cron-*` siblings (#456, #454, #452, #450, #448, #444, #442, #440, #437, #435, #430, #428, #426, #425, #423, #421, #420, #418, #416, #414, #406, #404, #401, #399, #397, #395, #393, #379). Keep #336 — it is plan content, not a fingerprint.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR is intentionally **NOT** labeled `ready-for-review`. Janitor decides.

## Tags

- `agent-job:stdewg`
- `oldest:one_track-2`

---

_Generated by stateless CRON `stdewg` (one-shot, LIFO issue-owner routine — claude-opus-4-7)._
