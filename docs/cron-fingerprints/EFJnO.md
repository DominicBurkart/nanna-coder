# Cron fingerprint — `EFJnO`

- job-id: `EFJnO`
- oldest-issue-id: `one_track#2`
- run-date: `2026-05-16`
- base: `main @ c71e6114d66c39dbf82525183e28d0a9fbb45f4f`
- prior fingerprints covering this repo: `X16QR` (PR #393), `xR5Hn` (PR #379),
  `fyhme` (PR #336)

## Scope

This is the **nanna-coder** slice of the 15-oldest `author:DominicBurkart`
filter. Six in-scope issues: #5 (CI monitoring), #10 (CI docs),
#20 (entity-management EPIC), #23 (AST entities), #24 (test entities),
#39 (Ollama → vLLM migration).

## Per-issue disposition

| issue | canonical PR     | head sha   | mergeable | disposition |
|------:|-----------------:|------------|-----------|-------------|
| #5    | **#247**         | `8a589694` | behind    | NO-OP — defer to #247 |
| #10   | **#254**         | `d9ccf860` | behind    | NO-OP — defer to #254 (blocked on `workflows`-scope token) |
| #20   | EPIC             | n/a        | n/a       | EPIC — defer to children #23, #24 |
| #23   | **#258**         | `7c2b7ed9` | **dirty** | NO-OP — defer to #258 (partial; codecov 92.70%) |
| #24   | **#267**         | `f229f365` | behind    | NO-OP — defer to #267 (partial; codecov 80.88%) |
| #39   | plan in **#336** | `e270a211` | behind    | NO-OP — plan-only PR; phase 1 awaits human go-ahead |

## Stale-head check vs prior cron `X16QR` (2026-05-15)

All five canonical-PR heads are **identical** to X16QR's fingerprint
(#247 `8a589694`, #254 `d9ccf860`, #258 `7c2b7ed9`, #267 `f229f365`,
#336 `e270a211`). Main is also unchanged at `c71e6114`. This is a
true byte-equivalent no-op of X16QR's disposition.

## Janitor carry-over

- All four code-bearing PRs (#247, #254, #258, #267) need rebase
  against current `main`. PR #258 requires conflict resolution
  (true `dirty` state from `behind`).
- PR #254 remains blocked on a `workflows`-scope GitHub token to
  land the `docs-check` CI wire-up. This cron's token also lacks
  that scope — blocker carries.
- PR #258 / #267 codecov diff flags (92.70% / 80.88%) carry from
  `fyhme`. Specific uncovered paths itemized in
  `docs/cron-fingerprints/fyhme.md`.
- #39 (Ollama → vLLM) plan in PR #336 stands. Issue prompt's
  premise about a hypothetical PR #140 is factually incorrect —
  human should confirm intent before phase 1 begins.
- **Close** prior nanna-coder fingerprint PRs (#393 X16QR, #379 xR5Hn,
  #336 fyhme plan-only) — this PR is the current snapshot, and
  #336's phase-1 plan content has been preserved in
  `docs/cron-fingerprints/fyhme.md` on its branch.

## Duplicate-on-sight tag

This PR's disposition is byte-equivalent to X16QR (PR #393). Janitor
may close this PR after closing the prior three.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR
is intentionally **NOT** labeled `ready-for-review`. The janitor decides.

---

_Generated by stateless CRON `EFJnO` (claude-opus-4-7 one-shot)._
