# CRON QkcXO — nanna-coder slice

- job-id: `QkcXO`
- oldest-issue-id: `one_track#2`
- parent-fingerprint: #420 (`VFjIU`, 2026-05-27)
- trigger: oldest-15 `author:DominicBurkart` across managed repos
- date: 2026-05-28

## Scope

Issues #5, #10, #20 (epic), #23, #24, #39 — the nanna-coder share of the
15 globally-oldest open issues authored by `DominicBurkart` across
managed repos. Sibling slices for this same trigger are emitted in
`one_track` (#2–#9) and `velib-mcp` (#9).

## Fingerprint result — zero drift

| issue | canonical PR | head SHA @ `VFjIU` | head SHA @ `QkcXO` | Δ | disposition |
|------:|-------------:|--------------------|--------------------|---|-------------|
| #5  | #247 | `8a589694` | `8a589694` | — | NO-OP — defer (partial; blocked on #243 + `workflows` token scope) |
| #10 | #254 | `d9ccf860` | `d9ccf860` | — | NO-OP — defer (blocked on `workflows` token scope) |
| #20 | EPIC (no PR) | n/a | n/a | — | NO-OP — epic; defer to leaves #23, #24 |
| #23 | #258 | `7c2b7ed9` | `7c2b7ed9` | — | NO-OP — defer (dirty; codecov diff 92.70%) |
| #24 | #267 | `f229f365` | `f229f365` | — | NO-OP — defer (behind; codecov diff 80.88%; blocked on #23) |
| #39 | #140 (code) + #336 (plan) | `79c94397` / `e270a211` | `79c94397` / `e270a211` | — | NO-OP — defer to #140 (phase-1 OpenAI-compat) + #336 (multi-PR plan) |

Base `main` unchanged @ `c71e6114`. **True idempotent no-op re-run.**
Latest in a chain of byte-identical no-op fingerprints (`VFjIU`,
`TzdJX`, `HTzrH`, `jYrHy`, `dRcjT`, `fyhme`, `myPy6`, `pZdbm`, `MvlE2`,
`dRwfn`, `EFJnO`, `X16QR`, `xR5Hn`, ...).

## Owner-assign clause

Not triggered — every in-scope issue has prior work. No assignment
mutations performed.

## Why this CRON does not ship code

1. Canonical PRs exist for every code-bearing in-scope issue. Pushing
   parallel implementations would create duplicate-PR sprawl (rule 2).
2. #247 / #254 / #258 / #267 / #140 are partial-progress PRs authored by
   other agent jobs; this cron does not push to branches it does not
   author.
3. #254's blocker is a token-permission problem (`workflows` scope) —
   this cron's token also lacks that scope.
4. #39's prompt premise is factually wrong (no merged `OpenAiProvider`);
   #140 has been advancing the actual phase-1 work — human-in-the-loop
   should confirm intent before phase 2.

## Janitor carry-over

- Rebase #247, #254, #258, #267, #140 against current `main`; #258 and
  #140 need conflict resolution (`dirty`).
- #254's `docs-check` wiring needs a maintainer token with `workflows`
  scope.
- Close superseded `affectionate-hawking-*` fingerprint PRs once this
  lands: #420 (`VFjIU`), #418 (`TzdJX`), #416 (`HTzrH`), #414 (`jYrHy`),
  #406 (`dRcjT`), #404 (`myPy6`), #401 (`pZdbm`), #399 (`MvlE2`),
  #397 (`dRwfn`), #395 (`EFJnO`), #393 (`X16QR`), #379 (`xR5Hn`). Keep
  #336 for its plan content but supersede its fingerprint role.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR
is intentionally **NOT** labeled `ready-for-review`. The janitor decides
promotion.

## Tags

- `agent-job:QkcXO`
- `oldest:one_track-2`

---

_Generated by stateless CRON `QkcXO` (claude-opus-4-7 one-shot, LIFO
issue-owner routine)._
