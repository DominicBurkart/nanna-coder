# Cron `a9xgz9` — nanna-coder slice

- job-id: `a9xgz9`
- oldest-issue-id: `one_track#2`
- parent-fingerprint: `stdewg` (PR #463)
- tick-date: 2026-06-25
- scope: oldest 15 author:DominicBurkart open issues across scoped repos
- slice: nanna-coder (6 issues: #5, #10, #20 epic, #23, #24, #39)
- sibling slices: velib-mcp (#9), one_track (#2–#9)

## Fingerprint result — byte-identical no-op vs `stdewg`

| issue | canonical PR              | head SHA @ `stdewg`     | head SHA @ `a9xgz9`     | Δ |
|------:|---------------------------|-------------------------|-------------------------|---|
| #5    | #247                      | `8a589694`              | `8a589694`              | — |
| #10   | #254                      | `d9ccf860`              | `d9ccf860`              | — |
| #20   | EPIC (no PR)              | n/a                     | n/a                     | — |
| #23   | #258                      | `7c2b7ed9`              | `7c2b7ed9`              | — |
| #24   | #267                      | `f229f365`              | `f229f365`              | — |
| #39   | #140 + #336               | `79c94397` / `e270a211` | `79c94397` / `e270a211` | — |

Base `main` unchanged at `c71e6114` since `yuzy9i` (≥10 days). **No canonical PR head moved between `stdewg` and `a9xgz9`.**

## Per-issue disposition (unchanged from `stdewg`)

| issue | disposition |
|------:|-------------|
| #5  | NO-OP — defer to #247 (`behind`; blocked on #243 + `workflows` token scope). Cron sibling PRs #412 (Rust-side ci_metrics parser) layer on top. |
| #10 | NO-OP — defer to #254 (`behind`; blocked on `workflows` token scope). Cron sibling PR #408 layers docs dedup. |
| #20 | NO-OP — epic; defer to leaves #23, #24 |
| #23 | NO-OP — defer to #258 (`dirty`; codecov diff 92.70%). Cron sibling PR #409 layers diff-coverage close. |
| #24 | NO-OP — defer to #267 (`behind`; codecov diff 80.88%; blocked on #23). Cron sibling PR #411 layers diff-coverage close. |
| #39 | NO-OP — defer to #140 (`dirty`) + #336 (plan, `behind`). Multi-PR chain (#370 #378 etc.) carries the vLLM migration; all still draft. |

## Why this CRON ships no code

1. Canonical PRs exist for every code-bearing in-scope issue; parallel implementations create duplicate-PR sprawl (CRON rule 2).
2. #247 / #254 / #258 / #267 / #140 are partial-progress PRs authored by other agent jobs; this cron does not push to branches it does not own.
3. #254's blocker is a token-permission problem (`workflows` scope) — this cron's token also lacks that scope.
4. #258 (`dirty`) and #140 (`dirty`) need rebase by their original authors before any code can be layered.

## Janitor carry-over

- Rebase #247, #254, #267 against current `main` (`c71e6114`).
- Resolve conflicts on #258 (`dirty`) and #140 (`dirty`).
- #254's `docs-check` wiring needs a maintainer token with `workflows` scope.
- Close superseded `claude/affectionate-hawking-*` fingerprint PRs on sight — keep `a9xgz9` (this PR), close `stdewg` (#463) and ALL earlier `cron-*` siblings.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR is intentionally **NOT** labeled `ready-for-review`. Janitor decides.

## Tags

- `agent-job:a9xgz9`
- `oldest:one_track-2`

---

_Generated by stateless CRON `a9xgz9` (one-shot, LIFO issue-owner routine — claude-opus-4-7)._
