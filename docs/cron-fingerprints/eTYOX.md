# CRON fingerprint: `eTYOX` — nanna-coder slice

- **job-id**: `eTYOX`
- **oldest-issue-id**: `one_track#2`
- **parent-fingerprint**: #421 (`QkcXO`)
- **base**: `main` @ `c71e6114d66c39dbf82525183e28d0a9fbb45f4f`

## Scope

Stateless LIFO issue-owner CRON `eTYOX` covers the 15 oldest
`author:DominicBurkart` open issues across managed repos. This is the
**nanna-coder** slice (6 issues: #5, #10, #20 epic, #23, #24, #39).
Sibling slices for the same trigger are emitted in `one_track`
(#2–#9) and `velib-mcp` (#9).

## Drift table — zero drift since `QkcXO`

| issue | canonical PR        | head SHA @ `QkcXO` | head SHA @ `eTYOX` | Δ | disposition |
|------:|---------------------|--------------------|--------------------|---|-------------|
| #5  | #247 (`xM9Bl`)        | `8a589694`         | `8a589694`         | — | NO-OP — defer (partial; blocked on #243 + `workflows` token scope for CI-wiring follow-up) |
| #10 | #254 (`qsAIJ`)        | `d9ccf860`         | `d9ccf860`         | — | NO-OP — defer (blocked on `workflows` token scope) |
| #20 | EPIC (no PR)          | n/a                | n/a                | — | NO-OP — epic; defer to leaves #23, #24 |
| #23 | #258 (`03678285`)     | `7c2b7ed9`         | `7c2b7ed9`         | — | NO-OP — defer (dirty; codecov diff 92.70%) |
| #24 | #267 (`QO4Td`)        | `f229f365`         | `f229f365`         | — | NO-OP — defer (behind; codecov diff 80.88%; #259 superseded; blocked on #23) |
| #39 | #140 (code, `feat/openai-compat-gateway`) + #336 (plan) | `79c94397` / `e270a211` | `79c94397` / `e270a211` | — | NO-OP — defer to #140 (phase-1) + #336 (plan) |

Base `main` advanced from `196c39f3` (at PRs #247/#258/#267 filing time)
to `c71e6114` since the QkcXO run — PR #371 (`feat(eval): drive nanna
binary as subprocess for SWE-bench scoring`) landed. None of the
canonical PR heads moved; all six are in `behind`/`dirty` state pending
rebase by their original authors.

**True idempotent no-op re-run.** Latest in the chain of byte-identical
no-op fingerprints `QkcXO` ← `VFjIU` ← `TzdJX` ← `HTzrH` ← `jYrHy` ←
`dRcjT` ← `fyhme` ← `myPy6` ← `pZdbm` ← `MvlE2` ← `dRwfn` ← `EFJnO` ←
`X16QR` ← `xR5Hn` ← ... ← canonical implementations.

## Owner-assign clause

Not triggered — every in-scope issue has prior work and is already
assigned to `DominicBurkart`. No assignment mutations performed.

## Why this CRON does not ship code

1. Canonical PRs exist for every code-bearing in-scope issue. Pushing
   parallel implementations would create duplicate-PR sprawl (rule 2).
2. #247 / #254 / #258 / #267 / #140 are partial-progress PRs authored by
   other agent jobs (`xM9Bl`, `qsAIJ`, `03678285`, `QO4Td`,
   `feat/openai-compat-gateway`); this cron does not push to branches it
   does not author.
3. #254's blocker is a token-permission problem (`workflows` scope) —
   this cron's token also lacks that scope.
4. #39's prompt premise ("vLLM is likely to have broader GPU
   acceleration") interacts with active design churn around the
   OpenAI-compat gateway shape on #140; a fresh implementation would
   conflict with the in-flight phase-1 work. Human-in-the-loop should
   confirm intent before phase 2.
5. Main has advanced since the canonical PRs were filed (PR #371
   landed). Canonical PRs need rebase by their original authors before
   any further code can be layered on top.

## Janitor carry-over

- Rebase #247, #254, #258, #267, #140 against current `main`
  (`c71e6114`). #258 and #140 need manual conflict resolution
  (`dirty`).
- #254's `docs-check` wiring (13-line `ci.yml` patch in PR description)
  needs a maintainer token with `workflows` scope; current
  cron-token does not have it.
- #267's deferred follow-ups for #24 (`CoverageResult`, `SecurityAudit`,
  `TrendAnalysis`, flaky-test detector, pre-commit hook,
  `LintLocation.file` → `FileEntity.id` cross-ref [blocked on #23],
  proptest serde roundtrip) remain open as explicit follow-up PRs.
- Close superseded `affectionate-hawking-*` fingerprint PRs once this
  lands: #421 (`QkcXO`), #420 (`VFjIU`), #418 (`TzdJX`), #416 (`HTzrH`),
  #414 (`jYrHy`), #406 (`dRcjT`), and prior chain entries. Keep #336 for
  its plan content but supersede its fingerprint role.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR
is intentionally **NOT** labeled `ready-for-review`. The janitor decides
promotion.

## Tags

- `agent-job:eTYOX`
- `oldest:one_track-2`

---

_Generated by stateless CRON `eTYOX` (claude-opus-4-7 one-shot, LIFO
issue-owner routine)._
