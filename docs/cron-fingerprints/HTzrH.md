# CRON `HTzrH` — nanna-coder slice fingerprint

job-id: `HTzrH`
oldest-issue-id: `one_track#2`
predecessor (nanna-coder LIFO): `jYrHy` / PR #414 (2026-05-24)
window: 15 oldest `author:DominicBurkart` open issues across managed repos
this slice: nanna-coder #5, #10, #20 (epic), #23, #24, #39 (6 of 15)

## Per-issue disposition

Verified all canonical PR head SHAs on 2026-05-25. `main` unchanged at
`c71e6114`. Every head SHA byte-identical to the `dRcjT` snapshot — **zero
in-scope drift, true no-op re-run.**

| issue | canonical PR        | head SHA   | mergeable | disposition |
|------:|--------------------:|------------|-----------|-------------|
| #5    | #247                | `8a589694` | behind    | NO-OP — defer to #247 (partial; aggregator/dashboard follow-ups) |
| #10   | #254                | `d9ccf860` | behind    | NO-OP — defer to #254 (partial; needs `workflows` token scope to wire `docs-check`) |
| #20   | EPIC (no PR)        | n/a        | n/a       | NO-OP — epic; defer to leaves #23, #24 |
| #23   | #258                | `7c2b7ed9` | dirty     | NO-OP — defer to #258 (partial; codecov diff 92.70% < 100%) |
| #24   | #267                | `f229f365` | behind    | NO-OP — defer to #267 (partial; codecov diff 80.88% < 100%; blocked on #23) |
| #39   | #140 (code) + #336 (plan) | code: `79c94397` / plan: `e270a211` | dirty / behind | NO-OP — defer to #140 (phase-1 OpenAI-compat provider) and #336 (multi-PR plan) |

All six issues already assigned to `DominicBurkart` with prior work, so the
"assign owner when unassigned and unstarted" rule does not fire.

## Note on #39 canonical PR selection

Two LIFO predecessors disagree on the #39 canonical:

- `dRcjT` / `fyhme` cite **#336** (plan-only, no code).
- `jYrHy` cites **#140** (`feat: add OpenAI-compatible gateway provider`,
  current head `79c94397`, dirty).

Both are correct in their own frame: #140 is the *code* foundation for
phase 1 of the vLLM migration (OpenAI-compat provider); #336 is the
multi-PR *plan* artifact. Neither alone closes #39. This fingerprint
tracks both — janitor should keep both branches alive until phase 1
lands.

## Why this CRON does not ship code

1. Canonical PRs exist for every code-bearing in-scope issue. Pushing
   parallel implementations would create duplicate-PR sprawl (rule 2).
2. `#247` / `#254` / `#258` / `#267` / `#140` are partial-progress PRs
   authored by other agent jobs; per precedent this cron does not push
   to branches it does not author.
3. `#254`'s blocker is a token-permission problem (`workflows` scope),
   not an implementation problem — this cron's token also lacks that
   scope.
4. #39's prompt premise is factually wrong (no merged `OpenAiProvider`);
   #140 has been advancing the actual phase-1 work — janitor /
   human-in-the-loop should confirm intent before phase 2 begins.

Per-PR gaps (codecov diffs on #258/#267, the #254 `workflows`-scope
blocker, the #5 aggregator/dashboard follow-ups, the #39 multi-PR plan)
are itemised in prior fingerprints `dRcjT.md` / `fyhme.md` / `jYrHy.md`.
They are not re-posted as comments on the canonical PRs.

## Janitor carry-over

- Rebase `#247`, `#254`, `#258`, `#267`, `#140` against current `main`;
  `#258` and `#140` need conflict resolution (`dirty`).
- The `#254` `docs-check` wiring needs a maintainer token with `workflows`
  scope to push the 13-line workflow patch.
- Close superseded `affectionate-hawking-*` fingerprint PRs under tag
  `oldest:one_track-2`: `#414` (`jYrHy`), `#406` (`dRcjT`), `#404` (`myPy6`),
  `#401` (`pZdbm`), `#399` (`MvlE2`), `#397` (`dRwfn`), `#395` (`EFJnO`),
  `#393` (`X16QR`), `#379` (`xR5Hn`). Keep `#336` for its plan content but
  supersede its fingerprint role.

## Promote-to-human

Per CRON contract clause (3) — **planner jobs never promote**. This PR is
intentionally **NOT** labeled `ready-for-review`. The janitor decides.

## Tags

- `agent-job:HTzrH`
- `oldest:one_track-2`

---

_Generated by stateless CRON `HTzrH` (LIFO issue-owner routine, claude-opus-4-7 one-shot)._
